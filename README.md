Building a landing page to spec. 

This is to cement what is currently being learned in the intro to web course w/ _nology. 

It will contain a mixture of HTML, CSS, SCSS using Flexbox, and possibly CSS Grid. 